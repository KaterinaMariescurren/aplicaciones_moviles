// app/(map)/index.tsx
import React from 'react';
import { View, Text } from 'react-native';

const MapScreen = () => {
    return (
        <View>
            <Text>Pantalla de Zonas</Text>
        </View>
    );
};

export default MapScreen;
